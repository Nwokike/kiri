# Architecture Overview

> System design for Kiri Research Labs platform.

## Stack

```
┌─────────────────────────────────────────────┐
│                  Cloudflare                  │
│          (DNS, CDN, DDoS Protection)         │
└─────────────────────────────────────────────┘
                      │
┌─────────────────────────────────────────────┐
│                   Nginx                      │
│              (Reverse Proxy)                 │
└─────────────────────────────────────────────┘
                      │
┌─────────────────────────────────────────────┐
│                  Gunicorn                    │
│            (WSGI Application)                │
└─────────────────────────────────────────────┘
                      │
┌─────────────────────────────────────────────┐
│                   Django                     │
├─────────────┬─────────────┬─────────────────┤
│   Projects  │ Publications│     Users       │
│     App     │    App      │      App        │
└─────────────┴─────────────┴─────────────────┘
        │             │              │
┌───────┴─────────────┴──────────────┴────────┐
│                  SQLite                      │
│            (with R2 Backups)                 │
└─────────────────────────────────────────────┘
```

## Apps

| App | Purpose |
|-----|---------|
| `core` | Shared models, AI service, utilities |
| `projects` | Project showcase, Traffic Controller |
| `publications` | Research library, papers |
| `users` | Authentication, profiles |

## Traffic Controller

Routes projects to free external compute:

| Lane | Environment | Use Case |
|------|-------------|----------|
| A 🟢 | WebContainers (browser) | React, Vue, Node.js, SLMs |
| B 🟠 | Binder (cloud) | Django, Flask, Docker |
| C 🔴 | Colab (GPU) | PyTorch, Transformers |

→ See [TRAFFIC_CONTROLLER.md](./TRAFFIC_CONTROLLER.md)

## External Services

| Service | Purpose |
|---------|---------|
| Cloudflare R2 | Media storage, backups |
| Gemini API | AI classification |
| Groq API | AI fallback |
| GitHub Gists | Binder/Colab configs |

## Memory Budget (1GB VM)

| Component | RAM |
|-----------|-----|
| Django + Gunicorn | ~150MB |
| Huey (SQLite) | ~30MB |
| SQLite | ~20MB |
| OS | ~300MB |
| **Buffer** | ~500MB |
